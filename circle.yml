# circle.yml
# pywsse
# Author: Rushy Panchal
# Date: January 3rd, 2017
# Description: CircleCI configuration.

machine:
  environment:
    ENV: test
    BUILD_CI: true

  services:
    - postgresql

dependencies:
  pre:
    - pip install tox detox tox-pyenv
    - pyenv local 2.7.11 3.1.5 3.2.6 3.3.6 3.4.4 3.5.2

test:
  override:
    - detox
