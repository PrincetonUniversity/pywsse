# circle.yml
# pywsse
# Author: Rushy Panchal
# Date: January 3rd, 2017
# Description: CircleCI configuration.

machine:
  environment:
    ENV: test
    BUILD_CI: true

  services:
    - postgresql

dependencies:
  pre:
    - pip install tox
    - pyenv global 2.7.14 3.4.8 3.5.5 3.6.5

test:
  override:
    - tox
